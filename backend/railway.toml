[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn repomind.wsgi:application --bind 0.0.0.0:$PORT --workers 1 --worker-class gthread --threads 4 --timeout 300"
healthcheckPath = "/health/"
healthcheckTimeout = 180
restartPolicyType = "on_failure"
